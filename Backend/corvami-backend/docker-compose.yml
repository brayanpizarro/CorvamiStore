version: '3.8'

services:
  backend:
    build: .
    container_name: corvami-backend
    ports:
      - "6000:6000"
    environment:
      # ── BD propia (schema Ventas en si2 Neon) ────────────────────────────────
      - DB_HOST=ep-royal-glade-ac55fitc-pooler.sa-east-1.aws.neon.tech
      - DB_PORT=5432
      - DB_USER=neondb_owner
      - DB_PASSWORD=npg_V58gYFmBOPda
      - DB_NAME=si2
      - DB_SCHEMA=Ventas
      # ── BD externa (schemas Inventario y RRHH en si2 Neon) ───────────────────
      - EXT_DB_HOST=ep-royal-glade-ac55fitc-pooler.sa-east-1.aws.neon.tech
      - EXT_DB_PORT=5432
      - EXT_DB_USER=neondb_owner
      - EXT_DB_PASSWORD=npg_V58gYFmBOPda
      - EXT_DB_NAME=si2
      # ── Aplicación ────────────────────────────────────────────────────────────
      - PORT=6000
      - CLOUDINARY_CLOUD_NAME=def0eymwn
      - CLOUDINARY_API_KEY=298765669195532
      - CLOUDINARY_API_SECRET=7ybs0jIHP5eS2IO-XzcsWpTZPps
      - EMAIL_USER=soporteucenin@gmail.com
      - EMAIL_PASSWORD=xsdkpcsxftgnnfcn
      # ── APIs externas (otros proctors) ──────────────────────────────────────
      - INVENTORY_API_URL=http://localhost:5000
      - PURCHASE_API_URL=http://localhost:4000
    networks:
      - corvami-network

networks:
  corvami-network:
    driver: bridge
